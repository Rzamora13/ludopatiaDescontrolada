<form method="post" action="{{ path('app_t_i_c_k_e_t_delete', {'id': t_i_c_k_e_t.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_i_c_k_e_t.id) }}">
    <button class="btn">Delete</button>
</form>
